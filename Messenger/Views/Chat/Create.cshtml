@using System.Diagnostics.CodeAnalysis
@model Messenger.PL.ViewModels.CreateChatViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div>
    <h1>Start chat/group with:</h1>
    <form method="post">
        <div>
            <label class="form-label" asp-for="Users"></label>
            <select class="form-select" required asp-for="Users" asp-items="ViewBag.Users">
            </select>
        </div>


        <div>
            <input type="submit" class="btn btn-primary" value="Add" />
        </div>
    </form>
</div>
